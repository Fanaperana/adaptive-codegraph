//! # File-watching Daemon
//!
//! Watches the workspace for file changes and triggers incremental reindex.
//! Runs as a background process alongside the MCP server.

// TODO: Wire up notify file watcher + incremental reindex pipeline.

fn main() {
    eprintln!("adaptive-codegraph daemon — skeleton, not yet wired up");
    eprintln!("Will watch for file changes and trigger incremental reindex.");
}
