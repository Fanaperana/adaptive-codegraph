//! # MCP Server for adaptive-codegraph
//!
//! Exposes search, find_symbol, callers, callees, expand_neighborhood,
//! and semantic_search as MCP tools over stdio.

// TODO: Wire up rmcp server with actual tool implementations.
// This is a skeleton — the core library provides all the building blocks.

fn main() {
    eprintln!("adaptive-codegraph MCP server — skeleton, not yet wired up");
    eprintln!("Tools planned: search, semantic_search, find_symbol, find_callers,");
    eprintln!("  find_callees, expand_neighborhood, index, reindex_changed, index_status");
}
